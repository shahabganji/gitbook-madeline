**Name:**

BindingBehaviorDecorator

**Type:**

<T extends Constructable>(target: T) => T & IBindingBehaviorType<T>

**Initializer:**

<T extends Constructable>(target: T) => T & IBindingBehaviorType<T>

