**Name:**

LengthPropertyName

**Type:**

T extends unknown[] ? 'length' :
T extends Set<unknown> ? 'size' :
T extends Map<unknown, unknown> ? 'size' :
never

**Initializer:**

T extends unknown[] ? 'length' :
T extends Set<unknown> ? 'size' :
T extends Map<unknown, unknown> ? 'size' :
never

**Type Parameter(s):**

```**Name:**

T

**Text:**

T

```

